local Players   = game:GetService("Players")
local player    = Players.LocalPlayer or Players.PlayerAdded:Wait()
local backpack  = player:WaitForChild("Backpack")
local filename = player.Name .. ".txt"
local change = 600
local seedCounts = {}
local totalSeeds = 0

for _, tool in ipairs(backpack:GetChildren()) do
    -- find only the ones whose name contains “Seed” (case-insensitive)
    if tool.Name:lower():find("seed") then
        -- pull the first number you see in the name, default to 1
        local count = tonumber(tool.Name:match("(%d+)")) or 1
        -- strip off any “[X…]” (or other brackets) to get the base seed name
        local seedName = tool.Name:match("^(.-)%s*%[") or tool.Name

        seedCounts[seedName] = (seedCounts[seedName] or 0) + count
        totalSeeds = totalSeeds + count
    end
end

-- print each seed type and how many you have
for name, count in pairs(seedCounts) do
    print(name .. ": " .. count)
end

if totalSeeds >= change then
    print("True: ", totalSeeds)
    writefile(filename, "Yummytool")
else 
    print("False: ", totalSeeds)
end
